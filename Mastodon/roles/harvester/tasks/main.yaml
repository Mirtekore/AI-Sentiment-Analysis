- name: copy harvester.py to instance
  ansible.builtin.copy:
    src: harvester.py
    dest: dest_file/harvester.py

- name: run harvester
  shell: cd dest_file; nohup python3 harvester.py {{item.access_token}} {{item.mastodon_url}} &
  loop: "{{ mastodon }}"

